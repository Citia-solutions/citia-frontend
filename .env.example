# Plantilla de variables de entorno. Copia este archivo a `.env` y completa los valores.
# cp .env.example .env

# URL base del backend (sin slash final). Default si no se define: /api
VITE_API_URL=http://localhost:8000
